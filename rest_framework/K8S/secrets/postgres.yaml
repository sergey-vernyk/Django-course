apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret # Secret для PostgreSQL
type: Opaque # Стандартний тип секретів
stringData:
  # Змінні, які PostgreSQL читає під час старту контейнера
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
  POSTGRES_DB: django_db
